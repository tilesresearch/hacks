# wasm-layer

> Experiment with cross-platform unified layer for Wasm and WebGPU.

## Usage

To set up this project, run:

```cd single_component && \
cargo build && \
chmod +x build.sh && \
cd ../wasm-layer && \
cargo run```